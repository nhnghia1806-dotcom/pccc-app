services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pccc
    ports:
      - "5432:5432"
    volumes:
      - pccc_postgres_data:/var/lib/postgresql/data

volumes:
  pccc_postgres_data:
